# API endpoint - optional, auto-detected based on environment
# Local dev: defaults to http://localhost:3000
# Production: defaults to /api (same server)
# Override if needed: VITE_API_URL=https://custom-api.example.com
# VITE_API_URL=http://localhost:3000

